{% comment %}
  Main Static Page Section OS 2.0
{% endcomment %}

<section class="main-page py-12 md:py-20 bg-white">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <h1 class="font-heading text-3xl sm:text-4xl md:text-5xl font-extrabold text-gray-900 tracking-tight mb-8 text-center">
      {{ page.title }}
    </h1>
    <div class="prose prose-pink max-w-none text-gray-700 text-sm sm:text-base leading-relaxed space-y-4">
      {{ page.content }}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Main Static Page",
  "settings": []
}
{% endschema %}
